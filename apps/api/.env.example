# Server
PORT=4000
APP_ENV=development

# Database (Prisma + SQLite by default; swap to Postgres for production)
DATABASE_URL="file:./dev.db"

# Auth
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=1h

# CORS
ALLOWED_ORIGIN=http://localhost:3000
